<!--
Link the issue this closes. `Closes #12` makes GitHub close that issue when
this merges; a bare `#12` only cross-references it and closes nothing. It has
to be here in the description rather than in a commit, because this repository
squash-merges and the squash message is composed at merge time.

Delete the line if the change genuinely has no issue — a typo fix, a revert.
-->

Closes #

## What changed, and why

## Evidence

<!--
What you ran, and what it said. For a bug fix that means the check failing
before the change and passing after it: a test that passes either way has
demonstrated nothing.
-->
